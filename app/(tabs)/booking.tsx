import { Text, View } from 'react-native';

export default function BookingScreen() {
  return (
    <View className="flex-1 items-center justify-center bg-bg">
      <Text className="text-text-muted">Đặt lịch</Text>
    </View>
  );
}
