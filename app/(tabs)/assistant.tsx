import { Text, View } from 'react-native';

export default function AssistantScreen() {
  return (
    <View className="flex-1 items-center justify-center bg-bg">
      <Text className="text-text-muted">Trợ lý AI</Text>
    </View>
  );
}
