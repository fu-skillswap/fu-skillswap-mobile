import { Text, View } from 'react-native';

export default function ScheduleScreen() {
  return (
    <View className="flex-1 items-center justify-center bg-bg">
      <Text className="text-text-muted">Lịch của tôi</Text>
    </View>
  );
}
